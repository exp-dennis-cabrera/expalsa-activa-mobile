node_modules/
.expo/
dist/
web-build/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
.env
.env.local

# Proyectos nativos: EAS los borra y recrea en cada prebuild
# ("The android project is malformed, project files will be cleared"),
# asi que versionarlos no sirve de nada.
android/
ios/

# Credenciales de Firebase Cloud Messaging.
# En CI se reconstruye desde GOOGLE_SERVICES_BASE64 (ver app.config.ts).
google-services.json

.DS_Store
